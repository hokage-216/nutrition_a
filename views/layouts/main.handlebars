<!DOCTYPE html>

<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Nutrition A</title>
        <link rel="stylesheet" href="/css/jass.css">
        <link rel="stylesheet" href="/public/css/style.css">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    </head>

    <body>

        {{!-- If logged in display the dashboard --}}
        {{#if logged_in}}

            {{{ body }}}

        {{else}}

            {{!-- If logged out display the login partial --}}
            {{{ body }}}

        {{/if}}
        
        {{#if register}} <script src="../../public/js/register.js"></script> {{/if}}
        {{#if !logged_in}} <script src="../../public/js/login.js"></script> {{/if}}
        {{#if logged_in}} <script src="/js/logout.js"></script> {{/if}}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.min.js"></script>
    </body>

</html>